<!--
  使用说明（部署时此文件内容会自动推送给所有用户）：
  - 每次部署前，编辑下方内容，写好本次更新说明
  - 支持 Markdown 格式：标题（##）、列表（- ）、加粗（**text**）、行内代码（`code`）、链接（[text](url)）
  - 用 --- 分隔不同版本的记录，部署脚本只会提取第一个分隔线之前的内容
  - 部署后旧内容会自动保留在分隔线下方，作为历史记录
-->

## 视觉与配图专项（第五轮）

### 配图体系
- 首屏新增「研究前沿」概览：主题分布与关键词共现图**按当前筛选结果实时计算**（节点＝出现最多的关键词，连线＝同篇共现），不引入任何图片资源
- 关键词新增 6 类研究主题图标（储能 / 新能源 / 电网调度 / 电力电子 / 市场机制 / 高电压），卡片关键词行前显示该文献所属方向
- 空状态改为线稿插画：无匹配结果（放大镜）、无文献数据（收件盘）、无关键词（柱状图）三种场景，统一单色描边 + 虚线外圈
- 推送邮件改为品牌化研究简报：品牌抬头（与站点同一枚图形）+ 结构化正文（标题、列表、分隔线、加粗），不再是灰色等宽文本块；纯文本备选正文保留
- 品牌标识统一：顶栏与登录页改用与 favicon 相同的图形（新增 BrandMark 组件）
- 筛选面板加宽（上限 380 → 410px），字号放大后较长的刊名不再被截断

### 标题栏与统计
- 顶栏改为固定常驻：页面滚动时标题栏与统计条始终可见，内容区补齐等高留白
- 顶栏与正文之间加了明确分界（实边 + 投影）；品牌字号 28px 并加竖分隔线，导航 15px，统计胶囊 14px
- 统计条整体居中显示；顶栏高度 88 → 96px，侧栏吸顶偏移随之同步
- 「最近一周 / 一月新增」改为按**文献实际出版日期**统计；提前出版（Online First）按实际发布日期计入，标签相应改为「最近一周出版 / 最近一月出版」

### 卡片与详情
- 卡片信息行改为「出版日期 · 期刊徽标 · 期刊名」，日期不再挂在行尾，年份不再重复显示
- 新增期刊彩色徽标：一刊一色，卡片、详情弹窗、筛选面板三处一致
- 关键词只显示前 3 枚（软底无描边），其余折叠为「+N」
- 文献详情改为单列竖向阅读：有中文摘要时中英摘要左右两栏对照，没有中文摘要时正文占满整宽
- 关键词由窄栏改为横向元信息带；DOI 与卷期改为一行紧凑排列；弹窗宽度收至 920px
- 「打开原文」图标全站统一；文献推送页「收起筛选」与「快捷键」改为两列等宽并排

### 期刊分组
- 期刊按出版社分为 IEEE / 爱思唯尔 / 中文 / 其他四组，筛选面板与订阅设置一致，空分组不显示
- 每组使用低饱和索引色（石板蓝 / 陶土棕 / 松青绿 / 石墨灰），主色仍只用于可点击元素

### 交互与视觉收敛
- 全站字号统一放大一档，10px 一档并入 12px
- 交互元素统一过渡（含位移），修掉图标按钮悬停缩放「瞬跳」；卡片悬停不做位移
- 卡片操作图标常态可见，已读 / 收藏状态用颜色区分
- 17 处暖白底色统一为中性白，移除卡片上无效的装饰渐变
- 修复页面标题上方的角标与大标题挤在同一行的排版缺陷

---

## 视觉与配图专项（第五轮）

### 标题栏与统计

- 顶栏改为常驻固定：页面滚动时标题栏与统计条始终可见，内容区补齐等高留白，不会被遮挡
- 顶栏与正文之间加了明确分界（实边 + 投影）；品牌字号提到 28px 并加竖分隔线，导航 15px，统计胶囊 14px
- 统计条整体居中显示；顶栏高度由 88px 调整为 96px，侧栏吸顶偏移随之同步
- **「最近一周 / 一月新增」改为按文献实际出版日期统计**（此前按进入数据库的时间）。提前出版（Online First）的文献按实际发布日期计入，标题相应改为「最近一周出版 / 最近一月出版」

### 卡片与详情

- 卡片信息行改为「出版日期 · 期刊徽标 · 期刊名」，日期不再挂在行尾，年份不再重复显示
- 新增期刊彩色徽标：一刊一色，卡片、详情弹窗、筛选面板三处一致
- 关键词只显示前 3 枚（软底无描边），其余折叠为「+N」，点开详情查看全部
- 文献详情改为单列竖向阅读：**有中文摘要时中英摘要左右两栏对照，没有中文摘要时正文占满整宽**，不再预留空栏
- 关键词由窄栏改为横向元信息带；DOI 与卷期改为一行紧凑排列；弹窗宽度由 1180px 收至 920px
- 「打开原文」图标全站统一，详情与统计页不再使用易被误认成分享的图形

### 期刊分组

- 期刊按出版社分为 IEEE / 爱思唯尔 / 中文 / 其他四组，筛选面板与「文献推送」订阅设置保持一致，空分组不显示
- 每组使用低饱和索引色（石板蓝 / 陶土棕 / 松青绿 / 石墨灰），主色仍只用于可点击元素
- Journal of Modern Power Systems and Clean Energy 归入「其他期刊」

### 交互与视觉收敛

- 交互元素统一过渡令牌（含位移），修掉图标按钮悬停缩放「瞬跳」的问题；卡片悬停不做位移以避免整列抖动
- 卡片操作图标常态可见，已读 / 收藏状态用颜色区分
- 文献推送页的「收起筛选」与「快捷键」改为两列等宽并排，不再叠成上下两行
- 全站字号统一放大一档，10px 一档并入 12px，小字号不再低于可读下限
- 17 处暖白底色统一为中性白，移除卡片上无效的装饰渐变
- 修复页面标题上方的角标与大标题挤在同一行的排版缺陷

---

## PC 端展示层完善（第四轮）

- 顶栏改为双行：导航与操作在上，文献统计（总量 / 未读 / 已读 / 收藏 / 近一周 / 近一月新增）独立成行，1280×800 等小笔记本分辨率下统计数字不再整体消失
- 标题字体自托管：内置 Noto Serif SC 分片，Windows 上不再回退到点阵宋体，标题清晰且各机器渲染一致
- 新增统一操作提示：成功与失败使用不同颜色与图标、可手动关闭、多条提示不再互相覆盖，并被读屏播报
- 空状态改为可操作：无匹配结果时提供「清除全部筛选条件」，无数据时提供「立即刷新」
- 搜索框新增清除按钮与 `/` 快捷键提示；新增可展开的「快捷键」说明面板；检索结果数量变化会被读屏播报
- 浏览器前进 / 后退可在视图间切换；筛选条件写入地址栏，可直接复制链接分享某次检索结果
- 文献列表支持批量操作：悬停出现复选框，可批量标记已读、批量收藏，并导出 RIS / BibTeX
- 卡片关键词可点击，一键按该关键词收窄检索；已读条目降级显示，未读状态交由左侧色条与字重表达
- 时间改为相对显示（刚刚 / N 小时前 / N 天前，超过一个月回退为日期）
- 文献详情：打开原文常驻顶部；DOI 可一键复制；宽屏下改为双栏；补全过程改为骨架占位
- 关键词统计页：词云支持显示数量与排序切换、配色随主题统一、键盘可操作；修复统计筛选条留白与卡片内嵌链接的结构问题
- 性能：清理无效的背景模糊、`transition: all` 收敛为具体属性、去掉列表悬停位移、长列表启用 `content-visibility`
- 骨架屏改为与真实卡片同形状，首屏加载不再塌陷跳动
- 修复输入搜索词时列表被重置回 50 条、已加载长度与滚动位置丢失的问题

---

## 稳定性与部署改进

- 服务启动不再依赖启动目录：以其它工作目录启动时也能正确读取 `.env` 与 `data/`，修复由此导致的登录「外部服务暂时不可用」
- 新增可复现的重新部署脚本 `scripts/redeploy.ps1`（构建 / 测试 / 重启 / 冒烟自检）与部署手册 `DEPLOY.md`，部署流程全部固化为可执行步骤
- 修复 PowerShell 5.1 下部署脚本因缺少 BOM 而报语法错误的问题

---

## PC 端界面与交互升级

- 弹窗（文献详情、收藏分组、版本更新）支持 Esc 关闭、焦点锁定在弹窗内、关闭后焦点回到原位置，并锁定背景滚动
- 修复搜索高亮漏标问题：标题、摘要、关键词中的命中词会全部高亮，搜索 `C++`、`(a+b)` 等含特殊字符的词也不报错
- 新增页面错误兜底：单个视图出错时显示“页面出错了”并可一键重新加载，不再整页白屏
- 筛选面板改为吸附在左侧并独立滚动，期刊较多时不必整页滚动即可触达底部筛选项
- 新增文献列表快捷键：`J`/`K` 上下选择、`Enter` 打开详情、`R` 标记已读、`F` 收藏、`/` 聚焦搜索框
- 列表滚动到底部自动加载下一批，同时保留“加载下一批文献”按钮可手动触发
- 已读与收藏改为即时更新，列表不再整体重绘
- 顶栏在较窄屏幕下自动收起统计数字，导航项不再被挤出可视区域；窗口小于 1180px 时出现横向滚动条而不是裁切内容
- 页面按视图与图标库分包并懒加载，首屏加载更快

---

## 自动补全

- 文献主页面载入后自动创建批量任务，一次补齐当前批次缺少的摘要、中文标题、中文摘要和中文关键词
- 批量任务在后端限制并发、共享正在执行的文献任务并优先读取缓存，避免浏览器逐篇请求和出版社接口限流
- 已完成的文献会随任务进度直接显示在列表中，无需打开详情或刷新页面
- 列表接口现在直接返回中文摘要和中文关键词，已有翻译首次载入即可显示

## 操作优化

- 新增“批量补全当前批次”功能，并展示批量任务的完成数量和处理结果
- 缺少翻译的文献提供“获取中文翻译”或“获取中文摘要”重试入口
- 缺少摘要的文献提供“获取摘要与中文翻译”联合重试入口
- 新增中文摘要显示开关，默认关闭；中文期刊不重复显示中文摘要

---

<!-- 以下为历史更新记录，部署时不会推送给用户 -->

## 2026.06.12

- 新增关键词统计页，支持按期刊和时间筛选关键词频次
- 新增内容显示开关：可自由控制作者、关键词、摘要的显示
- 新增关键词和期刊多选筛选，支持按相关性排序
- 搜索词和选中关键词在文献中高亮显示
- 新增用户周报邮箱和意见反馈功能
- 页面导航移至顶部，筛选面板移至左侧
- 支持局域网内其他设备访问
